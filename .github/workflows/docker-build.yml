name: Build and Push Docker Images

on:
  push:
    branches: [ main, master ]
    paths:
      - 'Dockerfile*'
      - 'configs/**'
      - 'scripts/**'
      - '.github/workflows/docker-build.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'Dockerfile*'
      - 'configs/**'
      - 'scripts/**'
  workflow_dispatch:

env:
  REGISTRY: docker.io
  BASE_IMAGE_NAME: rustamgk/devenv

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        profile:
          - name: personal
            dockerfile: Dockerfile
            tag_suffix: ""
          - name: sarna
            dockerfile: Dockerfile.sarna
            tag_suffix: "-sarna"
          - name: sdui
            dockerfile: Dockerfile.sdui
            tag_suffix: "-sdui"
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.BASE_IMAGE_NAME }}${{ matrix.profile.tag_suffix }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ${{ matrix.profile.dockerfile }}
        platforms: linux/amd64,linux/arm64
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Generate artifact attestation
      if: github.event_name != 'pull_request'
      uses: actions/attest-build-provenance@v1
      with:
        subject-name: ${{ env.REGISTRY }}/${{ env.BASE_IMAGE_NAME }}${{ matrix.profile.tag_suffix }}
        subject-digest: ${{ steps.build.outputs.digest }}
        push-to-registry: true
